{
  "git_activity": {
    "name": "Git Activity",
    "description": "Git repository activity ingestion for the timeline"
  },
  "fields": {
    "timeline": {
      "git_activity": {
        "sensors": {
          "git_activity": {
            "enabled": {
              "label": "Enabled",
              "description": "Whether Git activity sync is active."
            },
            "repos": {
              "label": "Repository Folders",
              "description": "Select one or more Git repository folders to monitor."
            },
            "sync_interval_minutes": {
              "label": "Sync Interval (minutes)",
              "description": "How often to check for new Git activity."
            },
            "initial_sync_policy": {
              "label": "Initial Sync Scope",
              "description": "How much history to import the first time this source runs.",
              "options": {
                "full": "Sync full history",
                "lookback_days": "Sync recent days",
                "from_now": "Only new records from now on"
              }
            },
            "initial_sync_lookback_days": {
              "label": "Lookback Days",
              "description": "Used when the initial sync scope is set to recent days."
            },
            "session_window_minutes": {
              "label": "Session Window (minutes)",
              "description": "Git operations in the same repository within this window are grouped into one timeline session."
            },
            "max_messages_per_session": {
              "label": "Messages per Session",
              "description": "Maximum representative Git messages kept in each aggregated timeline session."
            },
            "l3_summary_enabled": {
              "label": "Include in L3 Summaries",
              "description": "Allow aggregated Git sessions to inform temporal memory summaries."
            },
            "sensitive_mode": {
              "label": "Sensitive Content Handling",
              "description": "How to handle commit messages containing sensitive content.",
              "options": {
                "redact": "Redact sensitive content",
                "block": "Skip the whole commit"
              }
            },
            "sensitive_keywords": {
              "label": "Additional Sensitive Keywords",
              "description": "Commit messages matching these keywords use the sensitive-content rule above. Built-in keywords include password, secret, token, and more."
            }
          }
        }
      }
    }
  },
  "content_blocks": {
    "repo": "Repository: {repo_path}",
    "operation": "Operation: {operation_type}",
    "operations": "Operations: {operations}",
    "commit": "Commit: {old_sha}..{new_sha}",
    "commit_range": "Commits: {first_sha}..{last_sha}",
    "time_range": "Time: {time_range}",
    "messages": "Messages: {messages}"
  },
  "activity_types": {
    "session": "Session",
    "checkout": "Checkout",
    "commit": "Commit",
    "merge": "Merge",
    "rebase": "Rebase",
    "other": "Other"
  }
}
